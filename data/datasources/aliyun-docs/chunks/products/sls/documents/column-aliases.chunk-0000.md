## 别名规范
在SQL标准中，列名必须由字母、数字和下划线（_）组成，且以字母开头。如果您在采集日志时设置了不符合SQL92语法的列名，则需要在配置索引时为目标列名设置一个别名。如何配置索引，请参见[创建索引](create-indexes.md)。
当日志中的原始列名特别长时，您也可以设置一个简短的别名，用于SQL分析。
重要
别名仅仅用于SQL分析，底层的存储仍然使用的是原始列名。即查询语句中使用原始列名，分析语句中使用别名。
例如，字段名称为client-ip（包含连字符，不符合SQL92语法），可将其别名设置为client_ip。
