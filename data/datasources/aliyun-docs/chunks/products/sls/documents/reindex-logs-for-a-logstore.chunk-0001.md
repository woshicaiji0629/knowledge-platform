## 费用
若当前LogStore的计费模式为按写入数据量计费时，重建索引将不产生费用。具体内容，可参见[按写入数据量计费](billing-per-amount-of-data-written.md)。
若当前LogStore的计费模式为按使用功能计费时，重建索引会另外产生索引费用（只收取一次）和存储费用（每小时计价一次），价格和正常索引一样。更多信息，请参见[按使用功能计费模式计费项](billable-items.md)。
删除索引任务后，新生成的索引数据也被删除，不再收取重建索引而产生的存储费用。
