## 计费组成
CDN计费分为，基础服务计费（必选）+增值服务计费（可选），计费项组成如下图：
[基础服务计费](billing-rules-of-basic-services.md)（必选）：CDN加速产生的核心费用。基础服务费需要在按流量计费和按带宽峰值计费和月结95带宽峰值计费中选择一种，默认为按流量计费。
[增值服务计费](billing-of-value-added-services.md)（可选，单独计费）：如使用了静态HTTPS请求、实时日志投递等高级功能，会产生相应的额外费用。
