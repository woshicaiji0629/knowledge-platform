## 背景信息
阿里云CDN大部分节点已支持IPv6协议请求。开启IPv6后，用户在IPv6环境且就近CDN节点支持IPv6时，可通过IPv6协议访问CDN节点；若就近节点不支持IPv6，客户端仍可通过IPv4协议访问。
重要
部分域名为优化性能，后台默认关闭中国内地以外的IPv6。如您有中国内地以外的IPv6需求，请[提交工单](https://selfservice.console.aliyun.com/ticket/createIndex)。
